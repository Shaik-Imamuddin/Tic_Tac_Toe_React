# Tic_Tac_Toe_React

create a react app using below command

npx create-react-app toctactoe

replace the src file 

and run the project using below command

npm start

you need to be in parent directory of src before running the project